
1. cursor based pagination
2. create application in the beginning itself , through which users and applications can be managed. this is kind of super application, not created by any of the user.
ApplicationViewHelper.create_application(application_name="user_management", user_id = None, redirect_uris = "http://127.0.
0.1:8000/user_management", client_type = "public", grant_type = "password")